{
  "name": "responsive-image-generator",
  "version": "1.1.1",
  "private": "true",
  "description": "A plugin to extend Adobe® Photoshop's Generate Image Assets feature",
  "homepage": "https://github.com/DanMad/responsive-image-generator#readme",
  "bugs": "https://github.com/DanMad/responsive-image-generator/issues",
  "repository": {
    "type": "git",
    "url": "https://github.com/DanMad/responsive-image-generator.git"
  },
  "license": "MIT",
  "author": "DanMad",
  "scripts": {
    "bootstrap": "rm -rf node_modules && yarn install && husky install && chmod +x .husky/*",
    "build": "rm -rf dist && tsc --noEmit && babel src/index.ts --out-file dist/responsive-image-generator.min.jsx --extensions \".ts\" && uglifyjs dist/responsive-image-generator.min.jsx --compress --output dist/responsive-image-generator.min.jsx",
    "format": "sort-package-json && prettier --write \"**/*\""
  },
  "devDependencies": {
    "@babel/cli": "^7.14.8",
    "@babel/core": "^7.15.0",
    "@babel/preset-env": "^7.15.0",
    "@babel/preset-typescript": "^7.15.0",
    "husky": "^7.0.1",
    "lint-staged": "^11.1.1",
    "prettier": "^2.1.2",
    "sort-package-json": "^1.46.0",
    "typescript": "^4.0.3",
    "uglify-js": "^3.14.1"
  }
}
